const Cohorts = ({ cohorts, cohortListAside }) => {
  //   console.log(cohorts);
  cohortListAside()();
  return (
    <div className="aside">
      <h1>Cohorts</h1>
    </div>
  );
};

export default Cohorts;
